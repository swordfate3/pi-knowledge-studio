# Delivery acceptance — active work

User-confirmed priority: finish the five acceptance items below before pi-home/global storage changes. A passing module is not whole-product delivery. Last resumed: 2026-09-06.

## Gates

| Gate | Required evidence | Current status |
| --- | --- | --- |
| 1. Real Pi workflow | Actual installed Pi executes import, retrieval, generation and portable export; original bytes and citations checked; denial and restart recovery checked; distinguish real UI from headless automation | Current grounded-answer-v2 real Pi RPC workflow passed with actual WeMM and Qwen and explicit host controls: 14 tool operations, five denials, byte-checked original figures/citations/manifests and new-process recovery; generation/export 26008 ms, total 29428 ms (`/tmp/pi-e2e-uQGSuB/`). The earlier mixed-block AnswerWireResponse protocol passed a separate live run: generation/export 178156 ms, indexing 570 ms, query 455 ms, total flow 180851 ms. Artifacts: `/tmp/pi-e2e-AQEuMs/`. The earlier pre-contract run took 178057 ms after a 120-second failure. Both successful generation runs had little headroom under a deliberate 180000 ms model deadline. Automated real RPC UI responses and deterministic tool selection, not human TUI or autonomous quality proof. Local mock counters do not establish zero denied real-service requests. See `pi-e2e-validation.md` |
| 2. Real vision and rerank | Actual model inference through existing adapters, recorded model identity, correctly bound image/candidate outputs, explicit synthetic/public egress only | Partial live proof: pinned multilingual reranker completed actual CPU inference through HttpReranker in 1.473 seconds; relevant > busy-wait > unrelated for one synthetic Chinese case. Complete weights and configuration hashes verified; temporary server closed. Vision weights/configuration are now fully verified and actual describeImage inference passed the protocol in 32.884 seconds. However, SmolVLM described a red square and blue circle as a rectangle and blue sphere, omitting red and positions: fixture visual correctness failed. Both temporary services closed. Adapter execution is demonstrated, not visual-quality or production-service acceptance; see `live-model-validation.md` |
| 3. OCR | Pinned renderer, OCR engine and bilingual data; actual image-only English/Chinese recognition; explicit unverified-transcription provenance; original PDF retained | Opt-in full-document OCR implemented and real English/Chinese 1414×2000 scan ingest/search/labeled export passed, with native/scanned/overlay and whole-import blank-page rejection checks. Original PDF/render/TXT provenance retained. Independent audit found figure-only warning loss, occurrence-level geometry verification skipped by asset deduplication, and nested-signal cancellation gaps; all three reproduced gaps are fixed and covered by regressions. Temporary-path ancestor checks were also tightened without claiming descriptor-anchored isolation. Linux x64 external pinned stack currently temporary, not production deployment. See `ocr-usage.md` |
| 4. Held-out quality | Previously unused public sources, frozen questions/gold evidence, independent original figures, no-answer/hard negatives, ranking and image metrics, no test-set tuning | Public-source pilot complete: 8 answerable plus 3 corpus-relative absence cases, with failed acquisitions excluded explicitly. Lexical/live-WeMM locator recall .875/.9375; required-figure precision .5/.4, required occurrence recall 2/2 both. Search-only runner hard-coded abstained:false; this is NOT a measured generator abstention or hallucination rate. Explicit answer-versus-insufficient-evidence and none/selective/required figure policies are integrated and passed both mock regressions and one real Pi/Qwen flow. Textual requirement coverage cannot be replaced by a figure. These structural checks do not prove semantic sufficiency. The separate frozen answer-path set has 38 eligible cases of a 40-case target (two conflict cases deferred). Four dispatched cases produced zero valid answers/exports: two recorded 180-second timeouts, one coverage-validation error and one watchdog-interrupted unknown outcome, not retried. Those initial operational failures are preserved, not counted as correct abstentions. A separate current-wire/host-controls experiment on the same four now-exposed cases produced four valid results: A01 and A37 answered, A31 correctly abstained, and A25 falsely abstained despite the required original figure surviving capture, retrieval and model context. Required-figure recall was 0/1; no implementation loss was found in the A25 investigation. The two answered cases initially failed export because the evaluation harness omitted safe output-root creation. The runner is repaired; a separately recorded offline recovery exported both saved text-only answers (2/2), with no new inference or modification of original failures. The other 34 eligible cases remain unattempted and two conflict cases deferred. This is regression evidence, not a fresh held-out pass. Frozen artifacts remain unchanged. See `answer-acceptance-evaluation.md` and `public-corpus-evaluation.md` |
| 5. Long-term stability | Concrete concurrency/persistence/recovery/schema/export checks; repair demonstrated failure modes; document supported operating envelope | Both reproduced schema blockers fixed: v2 rejects unknown persistent triggers and guards revision INSERT replacement; legacy/v1 upgrades are validated and atomic. Main independently reran schema/transaction/process-concurrency suites: 18/18 passed plus typecheck. Four-process fresh/legacy/v1 opening, held-writer waiting and busy-timeout/retry are tested. Worker catalog suites: 24 passed, one ownership skip. This is not power-loss or privileged post-open DDL protection. The offline read-only verifier additionally checks retained inactive/active records, original blobs, OCR/rendition bindings and hints. It accepts token-equivalent valid schema without migrating the inspected DB; cumulative budget exhaustion and uncertifiable vector metadata report incomplete. Regressions cover no-write behavior and fixed exhaustion classification. See `storage-recovery.md`; no automatic GC, live backup or power-loss durability claim |

## Execution rules

- Continue independent gates when another gate is blocked.
- Report measured outcomes, not inferred completion. No automatic check-off from mock tests or successful registration.
- Keep existing private knowledge bases and remote workloads unchanged. Never stop another service to gain VRAM without explicit named approval.
- Keep source authority separate from model descriptions and OCR transcription.
- Do not count a headless grant as an actual human clicking an approval dialog.
- Do not silently widen restricted format support or relax parser/security checks to make tests pass.
- Existing working tree includes prior uncommitted development; retain it. Git publication and pi-home migration are outside this acceptance pass.

## Latest integrated regression

Main independently ran `npm run check` and the full suite with the real OCR stack required (`PI_KS_OCR_TEST_REQUIRED=1`, `PI_KS_OCR_TEST_CONFIG=/tmp/studio-ocr-config-font.json`): **216 tests, 214 passed, two environment ownership skips, zero failures**, including optional host-only generation controls, read-only retained-store verification and safe evaluation export-root creation. A telemetry timing test now checks ordered client-observed offsets rather than assuming server timer gaps must be observable under concurrent buffering; independent timeout checks remain. Independent focused review found no concrete controls blockers; 33 adapter, extension and telemetry tests passed. This includes schema repairs, OCR regressions, the new answer contract, per-request text-versus-image reference enums, content-free failure telemetry and actual Pi RPC with mock inference. A separate three-request synthetic latency experiment completed more quickly with explicit experimental controls, but both pipeline answers failed strict validation; zero valid answers/exports. After separating paragraphs from figure selections in `grounded-answer-v2`, a new synthetic required-figure request passed strict validation and original-PNG export checks in 26.329 seconds. The paired `none` policy request returned valid insufficiency in 13.687 seconds; the experiment expected an answered result and therefore still exited unsuccessfully overall. Both used an explicit experimental forwarding configuration, not production defaults. The new wire also passed real Pi RPC with mock inference; historical live runs are not used as proof of the new configuration. The separate current-wire, explicit-controls live Pi run passed: 14 registered-tool operations, five denial checks, original-PNG/citation/manifest verification and new-process recovery; generation/export took 26008 ms and the entire run 29428 ms. Artifacts: `/tmp/pi-e2e-uQGSuB/`. Host controls were explicitly configured as llama.cpp, maxTokens 2048, enableThinking false and reasoningBudgetTokens 0, with a 180000 ms deadline. This is a single synthetic real-service workflow, not a latency SLA, human TUI approval or autonomous answer-quality proof. See `generation-reference-validation.md`. Independent telemetry review found no concrete blockers. Telemetry adds diagnosis, not a demonstrated latency fix; read-only server inspection found unlimited prediction/reasoning defaults but does not establish the cause of previous timeouts. It is not a substitute for real vision/rerank, independent answer-quality evaluation, human TUI acceptance or production durability.

## Latest bounded follow-up

- Main reran TypeScript and the required-real-OCR full suite after the evaluator changes: **216 tests, 214 passed, two ownership skips, zero failures** (`/tmp/studio-delivery-latest-tests.log`).
- The next never-dispatched subset was frozen as A02/A26/A32/A03. It stopped **before inference**: Qwen was healthy and its slot idle, but GPU1 utilization was 73% with a separate Python workload (GPU0 83%). This was a conservative contention stop, not an observed occupied Qwen slot. No model requests or service changes; selected attempts remain 0/4 and prior unique dispatched coverage remains four IDs. See the addendum in `answer-acceptance-evaluation.md` before any separately authorized resumption.
- Packaging now explicitly includes only the operational `scripts/verify-collection.mjs`, not development evaluation scripts. Dry-run inspection found 84 files and no matched runtime/model artifacts. An actual temporary tarball was unpacked; its verifier CLI loaded and failed closed on an absent collection without creating it. This is a narrow packaging smoke test, not full clean-machine installation or deployment acceptance.

## Baseline

Before this acceptance pass: 117 tests, 116 passed, one ownership test skipped due to environment permissions. Real WeMM six authored cases and tiny Qwen generation/export passed previously, but are not held-out or full Pi operation evidence. See `IMPLEMENTATION-PROGRESS.md` for details.
